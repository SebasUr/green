"""Facility track (CERN CDC context).

Deferred to Milestones 3-4. The package and the ``FacilitySignal`` /
``DataQualityReport`` schemas exist now so the combined-window and simulation
contracts are stable, but no CDC cleaning, scoring or modelling is implemented
in Milestones 0-2.
"""
